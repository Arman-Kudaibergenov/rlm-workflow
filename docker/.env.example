# RLM Server Configuration
RLM_PORT=8200

# Embedding provider: fastembed | openai | ollama
RLM_EMBEDDING_PROVIDER=fastembed

# Default model: multilingual (Russian, English, 50+ languages), CPU-only, ~471MB
# Original rlm-toolkit default is all-MiniLM-L6-v2 (English only)
RLM_EMBEDDING_MODEL=paraphrase-multilingual-MiniLM-L12-v2

# OpenAI (if using openai provider)
# RLM_EMBEDDING_PROVIDER=openai
# OPENAI_API_KEY=sk-...
# RLM_EMBEDDING_MODEL=text-embedding-3-small

# Ollama (if using ollama provider)
# RLM_EMBEDDING_PROVIDER=ollama
# OLLAMA_BASE_URL=http://host.docker.internal:11434
# RLM_EMBEDDING_MODEL=nomic-embed-text
